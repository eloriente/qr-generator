// Vendors
import { DefaultTheme } from "styled-components";
// Constants
import spaces from './contants/ui-theme_spaces.constants';
import fontSizes from './contants/ui-theme_fontSizes.constants';
import fontFamily from './contants/ui-theme_fonts.constants';
import colors from './contants/ui-theme_colors.constants';

const theme: DefaultTheme = {
  spaces: spaces,
  fontSizes: fontSizes,
  fontFamily: fontFamily,
  colors: colors
};

export default theme;